# 将仓库内容视为不可信输入

Yuki-RepoGuide 接收任意公开仓库 URL，因此 README 等仓库内容可能包含错误、危险命令或针对模型的提示注入。系统只把仓库内容作为数据和导览证据：AI 不接触任何密钥，不获得工具、联网或命令执行能力，只能生成受约束的结构化解释；Java 后端负责校验输出与证据引用，Quick Start 命令只能原样引用来源并提示用户确认，系统不会自动执行。这会限制模型自由度并增加校验工作，但能防止陌生仓库借由 AI 获得系统权限或把无来源内容伪装成可信操作。
